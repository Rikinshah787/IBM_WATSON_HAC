fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
httpx
python-json-logger
aiofiles
python-multipart
pandas

# Optional: pandas for faster CSV processing (falls back to built-in csv module if not installed)
# pandas==2.1.3

